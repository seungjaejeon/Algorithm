#include<stdio.h>
int main()
{
	int i, j, N;
	scanf("%d", &N);
	for (i = 1; i < N+1; i++)
	{
		for (int k = 0; k < N - i; k++)
			printf(" ");
		for (j = 0; j < i; j++)
		{ 
			printf("*");
		}
		printf("\n");
	}
}
